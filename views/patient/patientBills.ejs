<%- include('../partials/header', {title: 'Bills', stylesheetName: 'styles', headerTitle: 'Bills' }) %>
<%- include('../partials/patientNav') %>

<div class="medication-container">
  <div class="top-strip"></div> <!-- White strip before tabs -->
  <div class="tabs">
    <div
      class="tab <%= activeTab === 'unpayed' ? 'active' : '' %>"
      onclick="location.href='/patient-Bills?tab=unpayed'"
    >
    unpayed
    </div>
    <div
      class="tab <%= activeTab === 'payed' ? 'active' : '' %>"
      onclick="location.href='/patient-Bills?tab=payed'"
    >
    payed
    </div>
  </div>

  <div class="content-body">
    <% if (activeTab === 'unpayed') { %>
    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>amount</th>
            <th>invoice</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>50$</td>
            <td>open file</td>
            <td class="pay-btn-cell"><button class="pay-btn">pay</button></td>
          </tr>
          <tr>
            <td>50$</td>
            <td>open file</td>
            <td class="pay-btn-cell"><button class="pay-btn">pay</button></td>
          </tr>
          
          <!-- Add more rows here -->
        </tbody>
      </table>
    </div>
    <% } else if (activeTab === 'payed') { %>
    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>date of payment</th>
            <th>amount</th>
            <th>invoice</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>22/9/18</td>
            <td>50$</td>
            <td>open file</td>
          </tr>
        </tbody>
      </table>
    </div>
    <% } %>
  </div>
  
</div>

<script src="js/main.js"></script>
<%- include('../partials/footer') %>
